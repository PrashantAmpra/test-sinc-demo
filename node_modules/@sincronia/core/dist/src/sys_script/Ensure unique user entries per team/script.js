"use strict";

(function executeRule(cur) {
  var memberGr = new GlideRecord('x_nuvo_csd_field_service_team_member');
  memberGr.addQuery('team', cur.getValue('team'));
  memberGr.addQuery('member', cur.getValue('member'));
  memberGr.query();

  if (memberGr.next()) {
    cur.setAbortAction(true);
    gs.addErrorMessage(gs.getMessage('CSD_DISP_DUPLICATE_TEAM_MEMBER'));
  }
})(current);