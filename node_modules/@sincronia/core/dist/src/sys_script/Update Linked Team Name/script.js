"use strict";

(function executeRule(cur) {
  var teamName = cur.getValue('name') || '';
  var linkedId = cur.getValue('related_group') || '';
  FieldServiceTeamUtils.updateLinkedGroup(linkedId, teamName);
})(current);