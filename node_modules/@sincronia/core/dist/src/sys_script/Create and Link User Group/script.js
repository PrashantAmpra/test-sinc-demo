"use strict";

(function executeRule(cur) {
  var teamName = cur.getValue('name') || '';
  var teamId = cur.getValue('sys_id') || '';
  FieldServiceTeamUtils.createAndLinkGroup(teamName, teamId);
})(current);