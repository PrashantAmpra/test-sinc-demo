"use strict";

(function executeRule(cur) {
  var member = cur.getValue('member') || '';
  var team = cur.team.getRefRecord();
  var groupId = team.getValue('related_group') || '';
  FieldServiceTeamUtils.deleteLinkedGroupMember(groupId, member);
})(current);