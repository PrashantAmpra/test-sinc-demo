"use strict";

(function executeRule(cur) {
  var groupId = cur.getValue('related_group') || '';
  var teamId = cur.getValue('sys_id') || '';
  FieldServiceTeamUtils.deleteLinkedGroup(groupId, teamId);
})(current);