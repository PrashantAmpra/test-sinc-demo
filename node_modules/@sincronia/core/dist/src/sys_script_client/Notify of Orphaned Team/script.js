"use strict";

function onLoad() {
  if ((g_form.getValue('related_group') === null || g_form.getValue('related_group') === '') && !g_form.isNewRecord()) {
    g_form.addErrorMessage(getMessage('CSD_DISP_TEAM_ORPHAN'));
  }
}